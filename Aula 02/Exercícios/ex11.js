const vt = 200;
const vr = 150;
const salario = 1200;

const total = (vt + vr + salario);
const total2 = (total * 0.20);
const total3 = (total - total2);
console.log("O salário com desconto é:", total3);